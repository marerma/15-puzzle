//import {newGame} from './Gameclass.js'
/*
class Component {
  constructor (width, height, color, x, y, ctx) {
    this.width = width;
    this.height = height;
    this.x = x;
    this.y = y;
    ctx.fillStyle = color;
    ctx.fillRect(this.x, this.y, this.width, this.height);
  }
}

/*function draw() {
  const canvas = document.querySelector('canvas');
  if (canvas.getContext) {
    const ctx = canvas.getContext('2d');
    let tileWidth = (getComputedStyle(canvas).width)
    const newTile = new Component(tileWidth, 100, 'black', 20, 20, ctx)
  }
}

/*function drawTiles() {
  //
  //let position = 0
   // for (let i = 0; i < level; i++) {
      let newTile = new Component(100, 100, 'green', 0, 2)
      //position += tileWidth * (i+1)
    }*/
  


/*
export {Component, draw}*/